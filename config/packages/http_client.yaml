framework:
    http_client:
        scoped_clients:
            devevents-api.client:
                base_uri: 'https://devevents-api.fr'
                headers:
                    Accept: 'application/json'
                    apikey: '%env(DEVEVENTS_API_KEY)%'
